# 服务器配置
PORT=5200
NODE_ENV=development

# 邮件服务配置 (用于发送订阅到期提醒)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# LLM API配置 (用于智能解析订阅)
OPENAI_API_KEY=your-openai-api-key
OPENAI_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1/chat/completions
OPENAI_API_MODEL=qwen-max-latest

# 数据库配置
# SQLite数据库默认保存在db/subscription_tracker.db
# 如需修改数据库路径，取消注释并修改以下行
# DB_PATH=custom/path/to/database.db
